# Backend Student Profile Tasks

- [x] Create `Course`, `CommunityPost`, and `Message` entities
- [x] Update `Assignment` entity with student submission fields (`dueDate`, `status`, `comments`, `attachedFiles`)
- [x] Create `StudentService` with TypeORM queries and fallback initial seed datasets
- [x] Create `StudentController` with `@Controller('api/student')` routes
- [x] Register `StudentModule` in NestJS `AppModule`
- [x] Verify backend compilation with `npm run build`
